{
  "name": "@openreview/action",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.mjs",
  "scripts": {
    "build": "tsdown src/index.ts --format esm --dts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^6.0.1",
    "@openreview/core": "workspace:*"
  }
}
