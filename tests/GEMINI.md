# Testing Instructions

- **AI Evaluations Mandate:** You MUST write AI evaluations in `tests/ai-evals/` for every new feature or LLM prompt change you build. These evaluations should empirically measure the success rate of the AI's capabilities against known baselines or fixtures.
- **Unit Tests:** Always add unit tests alongside your code in the appropriate `tests/` subdirectories.
